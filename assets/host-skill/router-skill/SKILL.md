# Router Skill

This router skill is managed by `gsr` host adoption.

- Host-local installs copy this payload into the host-owned skills directory.
- Uninstall removes only files tracked by the host adoption manifest.
- `/gsr` TUI and slash-command integration are host-owned, external, and live-synced via the session contract.
