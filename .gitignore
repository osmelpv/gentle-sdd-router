# Dependencies
node_modules/

# Environment and secrets
.env
.env.*

# Logs
*.log

# Engram memory (local only)
.engram/

# GGA config (local only)
.gga

# Agent instructions (generated per-machine)
AGENTS.md

# Migration backups (local per-project)
router/backups/
router/.migrations.yaml

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Docker (testing only)
Dockerfile
docker-compose.yml
docker-compose.override.yml
.docker/

# Coverage
coverage/
.nyc_output/

# Local task notes (not part of repo)
tareas/

# Build artifacts
dist/
*.tgz

# GSR Watchdog heartbeat files (runtime state, not committed)
.gsr/watchdog/

# Pre-compiled TUI plugin (generated from tui.tsx via npm run build:tui)
# Not committed — regenerate with: npm run build:tui
tui.js
tui.compiled.js
