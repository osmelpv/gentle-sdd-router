name: safety
debug_invoke:
  preset: sdd-debug-mono
  trigger: never
phases:
  orchestrator:
    - target: anthropic/claude-opus
      phase: orchestrator
      role: primary
      contextWindow: 200000
