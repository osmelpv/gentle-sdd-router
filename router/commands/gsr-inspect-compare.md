---
description: Compare two presets side by side
---

Run `gsr inspect compare $ARGUMENTS` to compare two routing presets.

Shows differences and similarities between presets across all phases.

CONTEXT:
- Working directory: !`echo -n "$(pwd)"`
- Command: !`gsr inspect compare $ARGUMENTS`
