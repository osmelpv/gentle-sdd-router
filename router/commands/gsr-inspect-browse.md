---
description: "[Inspect] Browse multimodel metadata for a preset"
---

Run `gsr inspect browse $ARGUMENTS` and display the multimodel metadata.

Shows the full metadata contract including models per phase, pricing, context windows, and capabilities.

CONTEXT:
- Working directory: !`echo -n "$(pwd)"`
- Command: !`gsr inspect browse $ARGUMENTS`
