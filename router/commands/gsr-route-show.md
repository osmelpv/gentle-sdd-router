---
description: Show resolved routes for current preset
---

Run `gsr route show` and display the resolved routes to the user.

CONTEXT:
- Working directory: !`echo -n "$(pwd)"`
- Command: !`gsr route show`
